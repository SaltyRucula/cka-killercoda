# Secrets exercises complete

You created a Secret from literals, decoded a stored value, exposed Secret keys as environment variables, and mounted Secret keys as files.

Remember: base64 makes data transport-safe; it does not make the data confidential by itself.
